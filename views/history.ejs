<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game History</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .table thead th {
            border-bottom: 2px solid #dee2e6;
        }
        .result-badge {
            font-weight: 600;
            padding: 4px 10px;
            border-radius: 12px;
        }
        .died { background: #f8d7da; color: #dc3545; }
        .completed { background: #d4edda; color: #28a745; }
        .win { background: #d4edda; color: #28a745; }
        .loss { background: #f8d7da; color: #dc3545; }
    </style>
</head>

<body>

<div class="container mt-5">
    <h2 class="text-center mb-4">Game History</h2>

    <div class="d-flex justify-content-end mb-3">
        <a href="/" class="btn btn-primary me-2">🏠 Main Menu</a>
        <a href="/game" class="btn btn-success">🎮 Play Game</a>
    </div>

   

    <table class="table bg-white rounded shadow-sm">
        <thead class="table-light">
            <tr>
                <th>Session</th>
                <th>Score</th>
                <th>Duration</th>
                
                <th>Hits</th>
                <th>Powerups</th>
                <th>Distance</th>
                <th>Pool</th>
                <th>Result</th>
                <th>Date</th>
            </tr>
        </thead>

        <tbody>
            <% history.forEach(session => { %>
                <tr>
                    <td><span class="badge bg-secondary"><%= session.session_id %></span></td>
                    
                    <td class="text-primary fw-bold fs-5">
                        🏆 <%= session.final_score %>
                    </td>

                    <td>⏱️ <%= session.duration_seconds %>s</td>

                    

                    <td style="color:#e83e8c">💥 <%= session.obstacles_hit %></td>

                    <td style="color:#fd7e14">⚡ <%= session.powerups_collected %></td>

                    <td style="color:#0d6efd">📏 <%= session.distance_traveled %>m</td>

                    <td>
                        <span class="badge <%= session.pool_name === 'Free Play' ? 'bg-success' : 'bg-warning text-dark' %>">
                            <%= session.pool_name %>
                            <% if (session.entry_fee > 0) { %>($<%= session.entry_fee %>)<% } %>
                        </span>
                    </td>

                    <td>
                        <span class="result-badge <%= session.game_result %>">
                            <%= session.game_result %>
                        </span>
                    </td>

                    <td>
                        <%= new Date(session.created_at).toLocaleString() %>
                    </td>
                </tr>
            <% }) %>
        </tbody>
    </table>

    <% if (!history || history.length === 0) { %>
        <div class="alert alert-info text-center mt-4">
            No game history found. Start playing!
        </div>
    <% } %>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
